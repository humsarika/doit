import React from 'react'

function Header() {
  let navbarStyle = {
    padding : '20px',
    height:'30px',
  }
  return (
    <nav style={navbarStyle}>
        <p>Get ready to complete your goals with us!</p>
    </nav>
  )
}

export default Header;
